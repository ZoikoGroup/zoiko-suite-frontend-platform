// Re-exported from the shared definitions so the Legal components and the
// platform pages cannot drift apart. Kept as a module rather than deleted
// because the Legal components import these names directly.

export {
  FIELD,
  LABEL,
  OPTIONAL,
  BANNER_SUCCESS,
  BANNER_NEUTRAL,
  BANNER_ERROR,
} from "@/components/admin/shared/form";
